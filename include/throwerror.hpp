#ifndef TEA_THROW_ERROR_HPP_INCLUDED
#define TEA_THROW_ERROR_HPP_INCLUDED
#include <string>

// This function is called when an error needs to be thrown
void throwError(const std::string &errorMessage);

#endif
