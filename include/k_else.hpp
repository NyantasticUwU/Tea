#ifndef TEA_K_ELSE_HPP_INCLUDED
#define TEA_K_ELSE_HPP_INCLUDED
#include <string>
#include <vector>

// Called when the elif keyword is called in tea
const bool kElse(int &line, const std::vector<std::string> &teafile, const int &teafileSize);

#endif
